// flightActions.js

import { setSearchID, setResults } from '../reducers/flightSliceNew';

// export const fetchFlightResults = (formData) => async (dispatch) => {
//   try {
//     // Perform API call here using formData
//     const response = await yourApiFunction(formData);

//     // Dispatch actions to set search ID and results in Redux store
//     dispatch(setSearchID(response.searchId));
//     dispatch(setResults(response.results));
//   } catch (error) {
//     // Handle API error, dispatch an error action if needed
//     console.error(error);
//   }
// };
